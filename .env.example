# AirMessage Connect Environment Configuration

# For production deployment with SSL certificate
# Point this to your PEM certificate file (containing both cert and private key)
SERVER_CERTIFICATE=/path/to/your/certificate.pem

# For Firebase integration (optional but recommended for production)
# Download from Firebase Console: Settings > Service Accounts > Generate new private key
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/firebase-service-account.json

# Server will run on port 1259 by default (hardcoded in Main.java)
# Make sure your reverse proxy forwards to this port
